package codurance.academyfinalboy.backend.actions;

import org.junit.jupiter.api.Test;

class CreateTeamShould {

  @Test
  void call_create_team_service() {
    /*var mockedTeamService = mock(TeamService.class);
    CreateTeam createTeam = new CreateTeam(mockedTeamService);

    createTeam.execute(request.team().name(), request.team().description());

    verify(mockedTeamService).createTeam()*/

  }
}
